# Runtime dependencies for the wall-detection companion service
# (pipeline/wall_service.py). CPU-only — no CUDA/ROCm needed.
# Tested versions (2026-07-22, Python 3.12):
onnxruntime>=1.20,<2      # tested 1.27.0
opencv-python-headless>=4.9  # tested 5.0.0 (headless = no GUI/X11 deps)
numpy>=2.0,<3             # tested 2.2.6
scikit-image>=0.24        # tested 0.26.0 — graph_infer needs skeletonize at runtime
# The HTTP server itself uses only the Python stdlib (http.server) — no Flask.

# Optional: faster ROCm-free GPU path on the RX 6600 (gfx1032) via Vulkan/RADV.
# Only needed for `wall_service.py --backend ncnn --vulkan` (see DEPLOYMENT.md §1).
# ncnn>=1.0.20240102   # bundles Vulkan support in the pip wheel
